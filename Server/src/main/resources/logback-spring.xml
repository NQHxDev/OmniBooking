<?xml version="1.0" encoding="UTF-8"?>
<configuration>

   <property name="LOG_PATH" value="logs" />
   <property name="LOG_FILE" value="omnibooking-server" />

   <!-- Console Appender -->
   <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
      <encoder>
         <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%X{requestId}] %-5level [%thread] %logger{36} - %msg%n</pattern>
      </encoder>
   </appender>

   <!-- Rolling File Appender -->
   <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
      <file>${LOG_PATH}/${LOG_FILE}.log</file>
      <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
         <!-- Daily rollover -->
         <fileNamePattern>${LOG_PATH}/${LOG_FILE}.%d{yyyy-MM-dd}.gz</fileNamePattern>
         <!-- Keep 30 days' worth of history -->
         <maxHistory>30</maxHistory>
         <totalSizeCap>1GB</totalSizeCap>
      </rollingPolicy>
      <encoder>
         <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%X{requestId}] %-5level [%thread] %logger{36} - %msg%n</pattern>
      </encoder>
   </appender>

   <!-- Logging levels -->
   <root level="INFO">
      <appender-ref ref="CONSOLE" />
      <appender-ref ref="FILE" />
   </root>

   <!-- Kafka silencing -->
   <logger name="org.apache.kafka" level="ERROR" />

   <logger name="org.springframework" level="INFO" />
   <logger name="com.omnibooking" level="DEBUG" />

</configuration>
